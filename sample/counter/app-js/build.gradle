import com.arkivanov.Js

if (!isTargetEnabled(Js.class)) {
    return
}

plugins.apply('org.jetbrains.kotlin.js')

kotlin {
    js(IR) {
        browser {
            binaries.executable()
        }
    }
}

dependencies {
    implementation project(":sample:counter:shared")
    implementation "org.jetbrains.kotlin-wrappers:kotlin-react:$jsKotlinReactVersion"
    implementation "org.jetbrains.kotlin-wrappers:kotlin-react-dom:$jsKotlinReactDomVersion"
    implementation "org.jetbrains.kotlin-wrappers:kotlin-styled:$jsKotlinStyledVersion"
    implementation "org.jetbrains.kotlin-wrappers:kotlin-css-js:$jsKotlinCssVersion"
    implementation(npm("react-hot-loader", "^4.12.20"))
    implementation "com.ccfraser.muirwik:muirwik-components:$jsMuirwikVersion"
}
